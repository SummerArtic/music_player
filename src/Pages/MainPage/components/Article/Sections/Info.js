import React from 'react';
import styled from 'styled-components';

const Info = (props) => {
  return (
    <InfoSection>

    </InfoSection>
  )
}

const InfoSection = styled.section`

`;

export default Info;